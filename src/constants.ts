export const portfolioNameRegex: RegExp = new RegExp(/^[a-zA-Z0-9]{4,}$/);
export const priceRegex: RegExp = new RegExp(/^[0-9]+(\.[0-9]+)?$/);
export const numberSharesRegex: RegExp = new RegExp(/^[0-9]+$/);
export const dateRegex: RegExp = new RegExp(/^[0-9]{4}-[0-9]{2}-[0-9]{2}$/);
